!!!
%html
  %head
    %title TinkerDB - all your parts
    = stylesheet_link_tag 'compiled/screen.css', :media => 'screen, projection'
    = stylesheet_link_tag 'compiled/print.css', :media => 'print'
    /[if lt IE 8]
      = stylesheet_link_tag 'compiled/ie.css', :media => 'screen, projection'
  %body.bp.three-col
    #container
      #header
        %h1 TinkerDB - All Your Parts Are Belong To Us
        %ul.navi
          %li= link_to "All Parts", parts_path
          %li.last
            - if logged_in?
              Hi, #{h(current_user.username)}
              (#{link_to "Log out", session_path, :method => :delete})
            - else
              link_to "Log in", new_session_path
          
      #content
        - flash.each do |type, message|
          %div{:class => type}
            &= message
        = yield
      #sidebar
        = yield :sidebar
      #footer
        &copy; Jan Krutisch
      
    
